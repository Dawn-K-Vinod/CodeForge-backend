services:
  - type: web
    name: codeforge-backend
    region: singapore  # Use the region closest to you
    env: docker  # Specify that this is a Docker-based service
    buildCommand: docker build -t codeforge-backend .
    envVars:
      - key: PORT
        value: 3000
